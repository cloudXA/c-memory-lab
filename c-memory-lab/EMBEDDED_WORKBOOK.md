# 嵌入式 C 扩展自测

## 内存与启动

- `.text/.rodata/.data/.bss` 分别通常放什么？
- `.data` 为什么需要从 Flash 复制到 SRAM？
- DMA 为什么不能继续使用已经返回函数的局部数组？
- map 文件能回答哪些 RAM/Flash 问题？

## 寄存器与并发

- `REG |= mask` 展开成哪三步？
- 什么是写 1 清零，为什么不能按普通变量理解？
- `volatile` 能保证什么，不能保证什么？
- ISR 和主循环共享“检查并清除”标志为什么可能竞态？
- 固定池相对 `malloc` 的优势和代价是什么？
- Tick 回绕时如何判断超时？

## UART 数据链

- 为什么 DMA 回调边界不等于协议帧边界？
- 环形缓冲区如何区分空和满？
- 缓冲区满时有哪些策略？
- 流式解析器为什么可以处理半包？
- 长度字段在使用前必须验证什么？
- 坏校验后如何重新同步？
- 双缓冲为什么仍可能溢出？
- 命令回调为什么必须验证 payload？

## 工程与测试

- 为什么业务层不应直接依赖 HAL 句柄？
- Fake 和真实硬件测试各自能发现什么？
- 断言和错误码的职责区别？
- ISR 日志为什么危险？
- RAM 预算为什么必须留安全余量？

## 最终口述

从 UART 引脚收到第一个字节开始，讲到命令回调修改业务状态，并说明每一步的数据对象、生命周期、执行上下文、错误路径和验证方法。
