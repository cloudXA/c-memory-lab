# C 内存模型学习笔记

> 基于各课 .c 文件的问答记录

---

## 一、printf 格式占位符

### 1.1 `%02x` 是什么意思？（第 8 行 `printf("%02x ", bytes[i])`）

`%02x` 拆解：
- `%x` — 以十六进制格式输出（小写字母 a-f）
- `02` — 最小宽度 2 位，不足时左边补 0

**举例对比**：

| 字节值（十进制） | `%x` 输出 | `%02x` 输出 |
|:---:|:---:|:---:|
| 10 | `a` | `0a` |
| 255 | `ff` | `ff` |
| 0 | `0` | `00` |

这样每个字节始终显示为整齐的 2 位十六进制数，方便查看内存原始数据。

### 1.2 `%p`、`%zu`、`%c`、`%#x`、`%.2f` 分别是什么意思？（第 20-22 行）

```c
printf("letter     %p   %zu     %c\n",   (void *)&letter, sizeof letter, letter);
printf("number     %p   %zu     %#x\n",  (void *)&number, sizeof number, number);
printf("decimal    %p   %zu     %.2f\n", (void *)&decimal, sizeof decimal, decimal);
```

| 占位符 | 参数类型要求 | 含义 | 示例输出 |
|--------|-------------|------|----------|
| `%p` | `void*`（必须显式转换） | 打印内存地址（十六进制） | `0x16b0a30f0` |
| `%zu` | `size_t` | 打印 `sizeof` 返回值 | `4` |
| `%c` | `int`（char 会整型提升） | 打印单个 ASCII 字符 | `A` |
| `%#x` | `unsigned int` | 带 `0x` 前缀的十六进制 | `0xff` |
| `%.2f` | `double`（float 会提升） | 保留 2 位小数的浮点数 | `3.14` |

> **注意**：`%p` 期望的是 `void*`，所以要把 `&letter`（类型是 `char*`）显式转换成 `(void*)`，否则属于未定义行为。

### 1.3 `(void *)&letter` 加这个转换是什么意思？

- `&letter` — 取 letter 的地址，类型是 `char*`
- `(void *)&letter` — 把 `char*` 转成通用的 `void*` 类型

因为 `%p` 严格要求参数是 `void*` 类型，虽然大多数编译器不报错，但类型不匹配在 C 标准中属于未定义行为。加上显式转换可以消除警告，也是标准写法。

---

## 二、小端序（Little Endian）

### 2.1 为什么 0x12345678 打印出来是 78 56 34 12？（第 7-9 行循环）

因为你的 Mac 是**小端序**架构（x86-64）。

**小端序规则**：最低有效字节（LSB）存在最低内存地址。

```
值:    0x12  34  56  78
       高位            低位（LSB）

内存地址  低  → → → → → → → →  高
存储字节  78      56      34      12
         低位                      高位
```

循环从低地址往高地址读，所以先读到 `78`，然后 `56`、`34`、最后 `12`。

**大小端对比**：

| 架构 | 0x12345678 内存排列 | 代表平台 |
|------|-------------------|---------|
| 小端序 | `78 56 34 12` | x86、x86-64、大多数 ARM |
| 大端序 | `12 34 56 78` | 某些嵌入式处理器、网络字节序 |

### 2.2 为什么是 78 56 而不是 7 8 5 6？

因为 **CPU 寻址的最小单位是 1 个字节（8 比特）**，不能只读半个字节。

```
0x78 = 0111 1000 （这是一个完整的字节）
       ↑     ↑
      7     8     两个十六进制位合在一起才是一个字节
```

一个十六进制位 = 4 比特，所以 1 字节 = 2 个十六进制位。`0x78` 是一个整体，不能拆成 `0x7` 和 `0x8` 来单独读取。

### 2.3 number = 256 时，为什么是 00 01 00 00？

```
256 = 0x100 = 0x00000100（4 字节 int，高位补零）

内存排列（小端序）：
地址  低 → → → → → 高
字节  00   01   00   00
      LSB           MSB
```

- 字节 0（最低位）：`0x00`
- 字节 1：`0x01`（1 × 256 = 256，进位到这里了）
- 字节 2、3：`0x00`（高位补零）

### 2.4 -1 的十六进制是什么？

`-1` 在 4 字节 int 中是 `0xFFFFFFFF`，小端序排列为 `ff ff ff ff`。

**原因 — 补码（Two's Complement）**：

```
 1  = 0x00000001
~1  = 0xFFFFFFFE   （按位取反）
+1  = 0xFFFFFFFF   （加一，这就是 -1）

验证：-1 + 1 = 0
0xFFFFFFFF + 0x00000001 = 0x00000000  ✓（进位溢出丢弃）
```

**4 字节 int 值域**：

| 值 | 十六进制 | 说明 |
|----|----------|------|
| 2147483647 | `0x7FFFFFFF` | 最大正数 |
| 1 | `0x00000001` | |
| 0 | `0x00000000` | |
| -1 | `0xFFFFFFFF` | 全 1 |
| -2147483648 | `0x80000000` | 最小负数 |

**判断正负的快捷方式**：最高位是 0 → 正数/零，最高位是 1 → 负数。

---

## 三、unsigned char* 逐字节读取技巧

### 3.1 循环怎么知道读几个字节？为什么一个地址刚好对应一个字节？

```c
for (size_t i = 0; i < size; ++i) {
    printf("%02x ", bytes[i]);
}
```

**循环次数**由 `size` 参数决定，调用时传的是 `sizeof number`（int 是 4），所以循环 4 次。

**一个地址一个字节**是因为 `unsigned char*` 的指针步长是 1：

```c
// C 标准规定：sizeof(unsigned char) == 1 恒成立
```

**指针运算公式**：`ptr + i` 的实际地址 = `原始地址 + i × sizeof(指针指向的类型)`

| 指针类型 | `ptr + 1` 跳多少字节 |
|----------|---------------------|
| `int*` | 4 字节 |
| `char*` / `unsigned char*` | 1 字节 |
| `double*` | 8 字节 |

所以把 `&number`（int*）转成 `unsigned char*` 后，`bytes[0]` 读第 1 个字节，`bytes[1]` 读第 2 个字节，每次刚好前进 1 字节。

---

## 四、十六进制与二进制的关系

### 4.1 为什么查看内存都用十六进制而不是十进制？

因为十六进制和二进制**天然对齐**：

```
1 字节 = 8 比特
1 个十六进制位 = 4 比特
所以 1 字节 = 2 个十六进制位
```

**对照表**：

| 十六进制 | 二进制 | 十进制 |
|:---:|:---:|:---:|
| `0x0` | 0000 | 0 |
| `0x5` | 0101 | 5 |
| `0xA` | 1010 | 10 |
| `0xF` | 1111 | 15 |
| `0xFF` | 1111 1111 | 255 |

看 `0xFF` 就知道 8 个比特全是 1；看十进制 `255` 完全看不出位模式。所以查看内存原始数据时，十六进制是最直观的选择。

### 4.2 输出前是先转成了十六进制吗？

对，`%02x` 就是告诉 `printf`："把这个整数值用十六进制格式显示"。

```
内存原始数据（二进制）
      ↓
bytes[i] 读出一个字节值（比如 120）
      ↓
%02x 格式化成字符串 "78"
      ↓
屏幕显示: 78
```

同一个值 120，不同格式输出不同：
- `%02x` → `78`（十六进制）
- `%d` → `120`（十进制）
- `%c` → `x`（ASCII 字符，120 对应字母 x）

数据本身没变，只是显示方式不同。

---

## 五、内存无类型 — 核心哲学

### 5.1 内存本身知道一组字节是整数还是浮点数吗？

**不知道。** 内存就是一堆无意义的字节。是**类型系统**赋予了它们含义。

同一段内存 `78 56 34 12` 可以有不同解读：

```
用 int* 读取     → 0x12345678（整数 305419896）
用 float* 读取   → 约 5.69e-28（按 IEEE 754 解码）
用 char[4] 读取  → 'x' 'V' '4' '\x12'（4 个 ASCII 字符）
```

这就是 C 语言灵活性的根源——你可以用不同的"视角"（指针类型）去解读同一段内存，但也因此容易写出 bug。

---

## 六、快速参考

### sizeof 常见值（macOS/Linux x86-64）

| 类型 | 大小 |
|------|:---:|
| char | 1 |
| short | 2 |
| int | 4 |
| long | 8 | macOS/Linux 上是 8，Windows 上是 4 |
| long long | 8 | 所有平台至少 8 字节 |
| float | 4 |
| double | 8 |
| 指针（任何类型） | 8 |

**定长类型（`<stdint.h>`）**：

| 类型 | 大小 | 说明 |
|------|:---:|------|
| `int8_t` | 1 | 有符号 8 位 |
| `uint8_t` | 1 | 无符号 8 位 |
| `int16_t` | 2 | 有符号 16 位 |
| `uint16_t` | 2 | 无符号 16 位 |
| `int32_t` | 4 | 有符号 32 位 |
| `uint32_t` | 4 | 无符号 32 位 |
| `int64_t` | 8 | 有符号 64 位 |
| `uint64_t` | 8 | 无符号 64 位 |

> 定长类型跨平台一致，不会像 `long` 那样在 Windows 和 macOS 上大小不同。

> `sizeof` 是编译时运算符，不是函数。`sizeof(char)` 恒为 1（C 标准强制）。

**注意**：`long` 的大小因平台而异，这是 C 语言跨平台陷阱之一：

| 平台 | `long` 大小 | `long long` 大小 |
|------|:---:|:---:|
| macOS / Linux 64 位 | 8 | 8 |
| Windows 64 位 | 4 | 8 |
| 32 位系统 | 4 | 8 |

`long long` 在所有平台上至少 8 字节（C 标准保证 ≥ 8）。写跨平台代码时建议用定长类型如 `int64_t`（第 9 课会讲）。

### 常用 printf 占位符速查

| 占位符 | 用途 |
|--------|------|
| `%d` | 有符号十进制整数 |
| `%u` | 无符号十进制整数 |
| `%x` / `%X` | 十六进制（小写/大写） |
| `%#x` | 带 0x 前缀的十六进制 |
| `%02x` | 2 位十六进制，不足补 0 |
| `%p` | 指针地址（需 void*） |
| `%zu` | sizeof 返回值 |
| `%c` | 单个字符 |
| `%f` | 浮点数 |
| `%.2f` | 浮点数（保留 2 位小数） |
| `%s` | 字符串 |

---

## 七、指针基础（02_pointers.c）

### 7.1 代码结构分析

```c
int x = 42;
int *p = &x;    // p 是指针变量，存的是 x 的地址
```

**三个关键值**（运行示例输出）：

| 表达式 | 含义 | 示例值 | 说明 |
|--------|------|--------|------|
| `x` | x 的值 | `42` | 普通变量存的数据 |
| `&x` | x 自己的地址 | `0x16f586458` | x 在内存中的位置 |
| `p` | 指针 p 的值 | `0x16f586458` | p 存的就是 x 的地址 |
| `&p` | p 自己的地址 | `0x16f586450` | p 本身也在内存中，有自己的地址 |
| `*p` | p 指向的值 | `42` | 解引用：去 p 存的地址里拿数据 |
| `sizeof p` | 指针变量的大小 | `8` | 64 位系统指针固定 8 字节 |

**关键理解**：指针变量 p 自己也有地址（`&p`），它和它指向的变量 x 是**两个独立的变量**，只是 p 里面存了 x 的地址而已。

### 7.2 通过指针修改变量

```c
*p = 99;   // 去 p 存的地址，修改那个地址里的值 → x 从 42 变成 99
```

运行后 `x = 99`，`*p = 99`。`*p` 就是"去 p 存的地址那里，操作那个地址里的值"，所以 `*p = 99` 等价于 `x = 99`。

### 7.3 指针可以改变指向

```c
int y = 10;
p = &y;    // p 改为指向 y（p 现在存的是 y 的地址，不再是 x 的地址）
*p = 88;   // 通过 p 修改 y
```

**常见误解纠正**：
- ❌ "y 的指针指向 p" — 方向反了！
- ✓ "p 指向 y" — p 里面存了 y 的地址，p 是箭头，y 是目标

**运行结果**：`y=88, *p=88, x=99`

注意 x 还是 99，没被影响——因为 p 已经不指向 x 了，`*p = 88` 改的是 y。

**核心比喻**：指针就像一个遥控器，改指向就是换一台电视控制。原来遥控 x，换到遥控 y，x 保持原样不变。

### 7.4 指针本质总结

```
指针变量 p 的内容 = 另一个变量的地址
*p               = 那个地址里存的值
&p               = 指针变量 p 自己的地址（p 也是变量，也在内存里）

方向关系：p ──指向──→ 目标变量
         指针         被指向的数据
```

| 操作 | 含义 | 影响谁 |
|------|------|--------|
| `p = &x` | 让 p 指向 x | 只改变 p 的值 |
| `p = &y` | 让 p 改指向 y | 只改变 p 的值 |
| `*p = 99` | 通过 p 修改目标 | 改变 p 当前指向的变量 |
| `x = 99` | 直接修改 x | 改变 x |

### 7.5 什么不能修改？

普通变量 `int x` 可以随时重新赋值（`x = 4` 完全没问题），但以下情况**不能修改**：

#### 1. 数组名不能重新赋值

```c
int arr[5] = {1, 2, 3, 4, 5};
arr = ???;   // ✗ 编译错误！数组名是常量指针，不能指向别处
```

#### 2. const 变量不能改

```c
const int x = 10;
x = 20;      // ✗ 编译错误！const 保护了值
```

#### 3. 字符串字面量不能改

```c
char *s = "hello";
s[0] = 'H';  // ✗ 运行崩溃！字符串字面量存在只读区域
```

但如果用字符数组就可以：
```c
char s[] = "hello";
s[0] = 'H';  // ✓ 可以，数组在栈上，可读写
```

**为什么有这种区别？**

| 写法 | 内存储存位置 | 可修改？ |
|------|------------|:---:|
| `char *s = "hello"` | 只读数据段（.rodata） | ✗ |
| `char s[] = "hello"` | 栈上 | ✓ |

#### 4. 通过指针修改 const 数据是危险的

```c
const int x = 10;
int *p = &x;   // 能编译但编译器会警告
*p = 20;       // 未定义行为！可能崩溃或无效
```

正确做法是用 const 指针保护：
```c
const int x = 10;
const int *q = &x;
*q = 20;       // ✗ 编译错误！const 指针不允许修改
```

**总结**：普通变量随便改，数组名、const 变量、字符串字面量不能改。最容易混淆的是数组名——它看起来像变量，实际是常量。

### 7.6 WORKBOOK 核心三问

#### Q1: `p`、`&p`、`*p` 各自是什么值？

| 表达式 | 含义 | 示例值 | 类比 |
|--------|------|--------|------|
| `p` | 指针 p 存的内容（x 的地址） | `0x16f586458` | 遥控器存的红外编码 |
| `&p` | 指针变量 p 自己的地址 | `0x16f586450` | 遥控器本身在抽屉里的位置 |
| `*p` | 去 p 存的地址里拿值（x 的值） | `42` | 按下遥控器，电视显示的内容 |

**关键**：p 自己也是一个变量，有自己的内存位置（`&p`），只是它里面存的是别人的地址。

#### Q2: 执行 `*p = 99` 修改的是 `p` 还是 `x`？

修改的是 **x**，不是 p。

```
*p = 99
 ↓
解引用：去 p 存的地址（0x16f586458，即 x 的地址）
       把那个地址里的值改成 99
       → x 从 42 变成 99
       → p 本身的值（存的地址）没变
```

`*p = 99` 等价于 `x = 99`。p 仍然指向同一个地址，只是那个地址里的内容变了。

#### Q3: 为什么 `int *p` 中的 `int` 很重要？

`int` 告诉编译器两件事：

**① 解引用时读多少字节**

```c
int *p = &x;
*p   // 从 p 存的地址开始，读 sizeof(int) = 4 个字节
```

不同类型的指针，读的字节数不同：

| 指针类型 | `*p` 读取字节数 |
|----------|:---:|
| `char *` | 1 |
| `short *` | 2 |
| `int *` | 4 |
| `double *` | 8 |

**② 指针运算时的步长**

```c
int *p;
p + 1   // 不是地址 +1，而是地址 + sizeof(int) = +4 字节

char *q;
q + 1   // 地址 + sizeof(char) = +1 字节
```

类型决定了指针的"视野宽度"（一次读多少）和"移动步幅"（跳多远）。这就是为什么不能把 `int *` 直接赋给 `double *`——类型不匹配会导致解读错误。
